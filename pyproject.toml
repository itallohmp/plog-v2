[project]
name = "plog"
version = "0.1.0"
description = ""
authors = [
    {name = "Itallo Polito",email = "itallomp@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi (>=0.137.1,<0.138.0)",
    "paramiko (>=5.0.0,<6.0.0)",
    "uvicorn (>=0.49.0,<0.50.0)",
    "wsl (>=0.12,<0.13)",
    "python-dotenv (>=1.2.2,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
